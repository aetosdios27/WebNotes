// src/app/components/extensions/table/TableRow.ts
import { TableRow as TiptapTableRow } from "@tiptap/extension-table-row";

export const TableRow = TiptapTableRow.extend({
  // Keep default behavior, extend later if needed
});
